apps:
  ingress-nginx:
    installed: true
  cert-manager:
    installed: true
  issuers-cert-manager:
    installed: true
    needs:
    - cert-manager/cert-manager
  secrets-gitea:
    installed: true
  gitea:
    installed: true
    needs:
    - gitea/secrets-gitea
